{
  "name": "mkmd",
  "scripts": {
    "test": "node src/smoke-test.js",
    "bundle": "esbuild src/index.js --outfile=src/bundle.js --bundle --tree-shaking=true --platform=node --external:domino --external:std"
  },
  "dependencies": {
    "parse5": "^7.1.2",
    "turndown": "^7.1.1",
    "xmlserializer": "^0.6.1"
  },
  "devDependencies": {
    "esbuild": "^0.15.15"
  }
}
